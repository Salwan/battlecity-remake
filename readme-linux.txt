Needed dependencies:
- Python 2.5
- Pygame 1.9.1

To run the game on Linux, give execute right to "battlecity.pyw" (chmod 0755) and execute it.
Or, from terminal write this:

/usr/bin/python battlecity.pyw

(If your python runtime is installed somewhere else, change the path)